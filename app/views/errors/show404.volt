
{{ content() }}

<div class="jumbotron">
    <h1>Pagina no encontrada</h1>
    <p>Lo sentimos, has accedido a una pagina que no existe o ha sido movida</p>
    <p>{{ link_to('index', 'Inicio', 'class': 'btn btn-primary') }}</p>
</div>
